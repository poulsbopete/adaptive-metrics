# Adaptive metrics sandbox prototype (archived)

This tree was an **offline-first** Instruqt layout (Ubuntu `sandbox` host, fixtures, five challenges) built before the canonical track existed on Instruqt.

The **authoritative** track for this repository is **`elastic/elastic-adaptive-metrics`** on Instruqt ([manage](https://play.instruqt.com/manage/elastic/tracks/elastic-adaptive-metrics)). The live track root lives under `instruqt/elastic-adaptive-metrics/` at repo root.

Reuse pieces from here if you spin up a **separate** Instruqt slug (for example `elastic-adaptive-metrics-offline`) later.
